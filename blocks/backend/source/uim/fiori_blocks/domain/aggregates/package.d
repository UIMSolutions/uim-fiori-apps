module uim.fiori_blocks.domain.aggregates;

/// Domain aggregates for the application.
/// Domain aggregates are clusters of related entities that are treated as a single unit for data changes.
/// They help to enforce consistency rules within the aggregate boundary and ensure that the domain model remains coherent.
/// By defining well-structured domain aggregates, developers can improve the maintainability, flexibility, and correctness of the domain layer.
/// In essence, domain aggregates provide a foundation for creating a flexible, maintainable, and high-quality domain layer that can effectively support the evolving needs of the business.
/// Examples of domain aggregates include an order with its line items, a customer with its addresses, and a product with its variants.